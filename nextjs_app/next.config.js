/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['s.yimg.com', 'media.wired.com', 'cdn.vox-cdn.com'],
  },
}

module.exports = nextConfig
